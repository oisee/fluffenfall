# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Fluffenfall is a web-based PSG (Programmable Sound Generator) audio file processor that applies various audio effects ("fluffs") to PSG files. It's built with React 15 and uses browserify/watchify for bundling.

## Key Commands

### Development
- `npm install` - Install dependencies (required before running)
- `npm start` - Run watchify to compile JSX files and watch for changes (outputs to `public/js/main.js`)
- Open `/public/index.html` in Chrome (recommended) or Safari to use the application

### Testing
- Manual tests: Run test scripts in `src/test/` with Node.js
  - `node src/test/test_applyFluff.js <input.psg> <fluff.json> [output.psg]` - Test complete processing pipeline
  - `node src/test/test_psg2raw.js <input.psg> [output.raw]` - Test PSG to raw conversion
  - `node src/test/test_raw2psg.js <input.raw> [output.psg]` - Test raw to PSG conversion
  - `node src/test/test_rawApplyFluff.js <input.raw> <fluff.json> [output.raw]` - Test fluff application on raw frames
  - `node src/test/test_create*.js <frames>` - Test fluff pattern creation (GoRound, Syncopa, Octaved, Identity)
- Shell tests: `sh src/test/sh_test_*.sh` (test identity transform, raw conversion round-trip, swap effects)

## Architecture

### Core Processing Pipeline
1. **PSG Files** (.psg) - Input audio format from chip music trackers
2. **Fluff Files** (.fluff.json) - JSON configuration files defining audio transformations
3. **Processing Flow**: PSG → Raw frames → Apply fluff transformations → PSG output

### Key Components

**Frontend (React)**
- Entry: `src/main.jsx` → `src/Routes.jsx`
- Main pages: FluffenfallPage (processor), EffectronPage, AboutPage
- File handling: FileFormPSG, FileFormFluff components
- Processing trigger: ApplyFluff component

**Core Library** (`src/lib/`)
- Format converters: `psg2raw.js`, `raw2psg.js`, `raw2chip.js`, `chip2raw.js`
- Frame structures: `ChipFrame.js` (audio frame data), `FluffFrame.js` (effect frame)
- Effect processors: `GoRoundFluff.js`, `SyncopaFluff.js`, `OctavedGoRoundFluff.js`, `IdentityFluff.js`
- Main processor: `applyFluff.js` - Applies fluff patterns to audio frames

### Data Structures

**ChipFrame** (`src/lib/ChipFrame.js`): Represents a single audio frame with:
- Channels a, b, c: Each with period (p), volume (v), tone enable (t), envelope enable (e), noise enable (n)
- Envelope channel: Period (p) and form (f)
- Noise channel: Period (p)

**FluffFrame** (`src/lib/FluffFrame.js`): Effect frame defining transformations with:
- Channel mappings (source, offset, period/volume modifications, shifts)
- Global settings affecting all channels
- Repeat count, skip, and duplicate flags

**FluffPattern** (`src/lib/FluffPattern.js`): Contains an array of FluffFrames with repeat counts

**Fluff Types**:
- GoRound: Circular channel rotation
- Syncopa: Syncopation patterns
- OctavedGoRound: Octave shifting with rotation
- Identity: No-op for testing

## File Formats

- `.psg` - PSG audio files (binary format)
- `.fluff.json` - Effect configuration files
- `.raw` - Intermediate raw frame format
- Test files in `public/test/` and `src/test/` for development

## Browser Compatibility

- Chrome: Full support (recommended)
- Safari: Partial support with some issues
- Other browsers: Not tested/supported

## Development Notes

- Build output goes to `public/js/main.js` (generated by watchify, not committed)
- React components use `React.createClass` (React 15 style)
- File processing happens entirely client-side using FileReader API
- Test PSG and fluff files are provided in `public/test/`